import { LoadingSpinner } from "./ui/loadingSpinner"
export default function Loading() {
  return (
    <div className="min-w-full min-h-full flex items-center justify-center"><LoadingSpinner className="" /></div>
  )
}
